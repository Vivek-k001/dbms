DELIMITER //
CREATE PROCEDURE membership_status (
IN cid INT ,
    OUT membership VARCHAR(20)
    )
    BEGIN
     	DECLARE cred INT;
     	
     	SELECT credits INTO cred
     	FROM customer 
     	WHERE customer_id = cid;
     IF cred > 5000 THEN 
     SET membershio = "platinum";
    ELSEFIF cred >= 1000 AND cred <= 5000 THEN 
    SET memership = 'GOLD';
   
   ELSE 
   SET membership = 'silver';
  END IF;
 END //
 DELIMITER;
